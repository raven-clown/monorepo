{
  "name": "snippet-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Snippet manager monorepo: shared core, VSCode extension, and Electron desktop app",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
